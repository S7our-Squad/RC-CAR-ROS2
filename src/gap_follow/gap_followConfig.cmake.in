# - Config file for the gap_follow package
# It defines the following variables
#  gap_follow_INCLUDE_DIRS - include directories for FooBar
#  gap_follow_LIBRARIES    - libraries to link against
#  gap_follow_EXECUTABLE   - the bar executable

set(gap_follow_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${gap_follow_DIR}/export_gap_follow.cmake")

# These are IMPORTED targets created by gap_followTargets.cmake


